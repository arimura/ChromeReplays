{
  "dependencies": {
    "esbuild": "0.17.17",
    "tslib": "^2.5.0"
  },
  "scripts": {
    "build": "esbuild src/admob/adding-customevent-replay.ts --bundle --platform=browser --outfile=docs/out.js --loader:.ts=ts --format=iife --global-name=chromeReplay",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^29.5.1",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0"
  }
}
